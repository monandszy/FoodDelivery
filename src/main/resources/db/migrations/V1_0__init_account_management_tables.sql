CREATE TABLE account_details
(
    id SERIAL NOT NULL,
    PRIMARY KEY (id)
);

CREATE TABLE seller
(
    id SERIAL NOT NULL,
    PRIMARY KEY (id)
);

CREATE TABLE client
(
    id SERIAL NOT NULL,
    PRIMARY KEY (id)
);